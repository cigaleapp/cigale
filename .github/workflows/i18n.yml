name: Translations

on:
  workflow_dispatch: 
  push:
    branches: [main]
  pull_request:
    branches: [renovate/wuchale-*]

env:
  ELECTRON_SKIP_BINARY_DOWNLOAD: 'true'

jobs:
  refresh:
    name: Refresh .po files
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ssh-key: ${{ secrets.AUTOCOMMITS_DEPLOY_KEY }}
      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # Run it 10 times to ensure it converges, as it's not that stable
      # See for example: https://github.com/cigaleapp/cigale/compare/e50dfe886...598dbcf
      - name: Run wuchale to update .po files.
        run: |
          for i in {1..10}; do 
            bun wuchale --clean --sync 
          done

      - name: Check if more than PO-Revision-Date changed
        id: changes
        run: |
          if git diff -I '^"PO-Revision-Date: ' --exit-code src/locales/*.po; then
            echo significant=false >> $GITHUB_OUTPUT
          else
            echo significant=true >> $GITHUB_OUTPUT
          fi

      - if: steps.changes.outputs.significant == 'true'
        name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@04702edda442b2e678b25b537cec683a1493fcb9 # v7
        with:
          commit_message: 🌐 Refresh translation files
          commit_options: --no-verify
          file_pattern: src/locales/*.po
